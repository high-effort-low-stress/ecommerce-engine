spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://${DB_HOST}:5432/ecommerce_engine
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    format_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
